#include <stdio.h>
#include <math.h>
#include <stdlib.h>


int main()
{
    // double x;
    // scanf("%lf", &x);
    // int ans = ceil(x); // 3.001 -> 4
    // int ans = floor(x); // 3.9 -> 3 
    // int ans = round(x); 3.4 -> 3, 3.5-> 4
    // double ans = sqrt(21); // 4.58
    // printf("%.2lf", ans);
    // int ans = pow(2,3); // 8

    // double a, b;
    // scanf("%lf %lf", &a, &b);
    // double ans = pow(a, b);
    // printf("%.2lf", ans);

    int ans = abs(-5); // 5
    printf("%d", ans);

    return 0;
}