#include <stdio.h>
#include <math.h>

int main()
{
    float n = 3.1;
    // int rs = ceil(n); // 4
    // int rs = floor(n); // 3
    // int rs = round(n); // 3 integer value
    // int rs = sqrt(16); // returns square root of 16 is 4
    int rs = pow(4,3); // 4*4*4 = 64
    printf("%d", rs);
    return 0;
}